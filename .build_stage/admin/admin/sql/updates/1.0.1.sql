-- --------------------------------------------------------
-- Database update script for com_ordenproduccion
-- Version: 1.0.1
-- Author: Grimpsa
-- Date: 2025-01-27
-- 
-- This is a sample update script for future versions
-- --------------------------------------------------------

-- Example: Add new column to existing table
-- ALTER TABLE `#__ordenproduccion_ordenes` ADD COLUMN `priority` enum('low','medium','high','urgent') DEFAULT 'medium' AFTER `state`;

-- Example: Add new index
-- ALTER TABLE `#__ordenproduccion_ordenes` ADD INDEX `idx_priority` (`priority`);

-- Example: Update configuration
-- UPDATE `#__ordenproduccion_config` SET `setting_value` = '1.0.1' WHERE `setting_key` = 'component_version';

-- Note: This is a template file for future database updates
-- Uncomment and modify the SQL statements as needed for specific updates
